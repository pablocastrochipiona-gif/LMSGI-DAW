Ejercicio 1:
for $ld in /libros/libro
return $ld/titulo

Ejercicio 2:
for $ld in /libros/libro
where $ld/precio > 20
return $ld/titulo

Ejercicio 3:
for $al in /alumnos/alumno
return $al/@id | $al/nombre

Ejercicio 4:
for $p in /productos/producto
order by $p/precio
return $p/nombre

Ejercicio 5:
let $emp := /empleados
return count($emp/empleado)

Ejercicio 6:
let $total := sum(/numeros/numero)
return <total>{$total}</total>

Ejercicio 7:
for $pp in /personas/persona
where $pp/edad >= 18
return $pp/nombre

Ejercicio 8: 
for $cur in /cursos
return distinct-values($cur/curso/nombre)

Ejercicio 9:
let $total := sum(/ventas/venta[producto = "A"]/cantidad)
return <total>{$total}</total>

Ejercicio 10:
for $p in /pedidos/pedido
where $p/total > 100 
return $p/cliente

Ejercicio 11:
for $p in /empresa/empleado
group by $publisher := $p/departamento="IT"
group by $publisher := $p/departamento="HR"
return
    <grupo categoria="{$publisher}">
        <departemento>{$p/departamento}</departemento>
        <nombres>
            {
                for $l in $p
                return <nombre>{data($l/nombre)}</nombre>
            }
        </nombres>
    </grupo>

Ejercicio 12:
let $total := sum(/empresa/notas/nota)
let $total1 := count(/empresa/notas/nota)
return <total>{$total div $total1}</total>

Ejercicio 13:
for $t in /tienda/producto
where $t[@categoria="A"]
return $t/precio

Ejercicio 14:
for $l in /libros/libro
return $l/titulo |  $l/autor

Ejercicio 15:
for $cl in /clases/clase
where $cl/nota > 5
return $cl/alumno

Ejercicio 16:
let $max := max(/biblioteca/libro/paginas)
for $b in /biblioteca/libro
where $b/paginas = $max
return <notaMayor>{$max}</notaMayor>

Ejercicio 17:
for $p in /pedidos/pedido
where $p/cliente="Ana"
for $etiqueta_cliente in $p/cliente
return
    <resultado>
    <cliente>{$etiqueta_cliente}</cliente>
        {
        <producto>{ $p/producto}</producto>
        }
    </resultado>
//Mejorar

Ejercicio 18:
let $total := sum(empresa/empleado/salario)
return $total

Ejercicio 19:
for $u in universidad/alumno
group by $publisher := $u/carrera="DAW"
let $total := count($u/carrera)
group by $publisher := $u/carrera="DAM"
let $total := count($u/carrera)
for $etiqueta_carrera in $u/carrera
return
    <grupo categoria="{$publisher}">
    
        <contador>
            {
               <cuenta>{$etiqueta_carrera} - {$total}</cuenta>
            }
        </contador>
    </grupo>

Ejercicio 20:
for $u in tienda/pedido
group by $publisher := $u/cliente="Ana"
let $total := sum($u/productos/producto/precio)
group by $publisher := $u/cliente="Juan"
let $total := sum($u/productos/producto/precio)
for $etiqueta_costo in $u/cliente
return 
    <grupo categoria="{$publisher}">
    
        <contador>
            {
              <cuenta>{$etiqueta_costo} - {$total}€</cuenta>
            }
        </contador>
    </grupo>
